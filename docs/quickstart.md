# Quickstart - Créer une branche et une Pull Request

## 1. Créer une branche

Depuis la racine du projet :

```bash
git checkout main
git pull origin main
git checkout -b feature/premier-league-analysis
